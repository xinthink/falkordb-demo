[project]
name = "falkordb-demo"
version = "0.1.0"
description = "FalkorDB Lite demo showcasing graph database operations with a movies dataset"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "falkordblite>=0.7.0",
]
